name: 🧬 SOVEREIGN HYPER-EVOLUTION ENGINE

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'README.md'
      - '.gitignore'
      - '*.bak'
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  evolution_cycle:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🛰️ Step 1 - Ingesting Source Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_TOKEN || github.token }}

      - name: 🐍 Step 2 - Stabilizing Python Environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 Step 3 - Injecting Necessary Libraries
        run: |
          python -m pip install --upgrade pip
          pip install requests google-genai google-generativeai groq pandas sqlalchemy GitPython
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          

      - name: 🧠 Step 4 - Executing Autonomous Self-Healing
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          rm -f *.bak
          python evolution_engine.py

      - name: 🏁 Step 5 - Manifesting Evolution (Auto-Commit)
        run: |
          git config --global user.name 'Sovereign-AI-Bot'
          git config --global user.email 'bot@sovereign.ai'
          
          # 1. Cleanup temporary backups
          rm -f *.bak || true
          
          # 2. Update .gitignore
          cat <<EOF > .gitignore
          node_modules/
          repo_sync/
          .env
          *.log
          *.bak
          __pycache__/
          *.pyc
          *.pyo
          *.pyd
          kaggle_deploy/
          EOF
          
          # 3. Process changes
          git add .
          
          # 4. Clean index without failing if files don't exist
          git rm -r --cached __pycache__/ || true
          git rm --cached *.pyc *.pyo *.pyd || true
          
          # 5. Commit and Force Push
          if ! git diff --cached --quiet; then
            git commit -m "🧬 [HYPER-EVOLUTION]: Auto-fix applied (429 Stability & Cache Clean) - $(date)"
            git push origin main --force
            echo "✅ [SUCCESS]: Evolution manifested."
          else
            echo "🛡️ [STABLE]: No changes needed."
          fi
